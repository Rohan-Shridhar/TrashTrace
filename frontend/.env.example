# Public environment variables (safe to expose to browser)
# Vite only exposes VITE_* variables to the browser

# Optional: Display label for delivery radius on the tracking page
VITE_DELIVERY_RADIUS_LABEL=500 metres
